using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GameStatus : MonoBehaviour {
    /*
    // singleton
    public static GameStatus sharedInstance = null;
    [Header("Players")]
    public Player mainPlayer = null;
    public Player secondPlayer = null;

    void Awake() {
        Debug.LogFormat("GameStatus Awake()");
        if (sharedInstance == null) {
            sharedInstance = this;
        } else if (sharedInstance != this) {
            Destroy(gameObject);
        }
    }
    */
}
